# ============================================================
# server/ — Ubuntu laptop / Linux edge
# Same Python packages as requirements.txt, plus system (apt) deps.
#
# 1) System packages (once):
#    sudo apt update
#    sudo apt install -y python3-venv python3-pip \
#        libgl1 libglib2.0-0 libsm6 libxext6 libxrender1 \
#        tesseract-ocr libzbar0
#
# 2) Python venv + wheels:
#    cd server
#    python3 -m venv venv
#    source venv/bin/activate
#    pip install -U pip
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#    pip install -r requirements-ubuntu.txt
#
# 3) Config:
#    cp .env.example .env
#    # set CLOUD_URL + EDGE_SYNC_SECRET
#
# 4) Run (load .env then start; use 5001 if cloud already uses 5000):
#    set -a && source .env && set +a
#    PORT=5001 python app.py
#
# No app code changes required for Ubuntu.
# ============================================================

-r requirements.txt
